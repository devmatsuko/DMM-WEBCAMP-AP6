<!--id="#book-comment-<%= @book.id %>"の部分をrenderで部分的更新を行う。-->
$("#book-comment-<%= @book.id %>").html("<%= j(render partial: 'comment', locals: {book: @book} )%>")
<!--エラーメッセージがある場合は、エラーメッセージを部分的更新を行う。-->
$("#error_explanation").html("<%= j(render 'layouts/errors', { obj: @book_comment }) %>")
<!--コメント入力フォームの中身を空にする。-->
$('#book_comment_comment').val("");